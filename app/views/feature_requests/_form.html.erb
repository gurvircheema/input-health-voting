<%= form_with(model: feature_request, local: true) do |form| %>
  <% if feature_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feature_request.errors.count, "error") %> prohibited this feature_request from being saved:</h2>

      <ul>
      <% feature_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :feature_request_name %>
  </div>

  <div class="field">
    <%= form.label :created_by_user_id %>
    <%= form.text_field :created_by_user_id, id: :feature_request_created_by_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
